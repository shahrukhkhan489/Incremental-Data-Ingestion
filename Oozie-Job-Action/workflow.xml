<!--******************************************-->
<!--workflow.xml                              -->
<!--******************************************-->
 
<workflow-app name="Incremental Data Ingestion" xmlns="uri:oozie:workflow:0.1">
    <start to="create_table_first_time"/>
    <action name="create_table_first_time">
        <shell xmlns="uri:oozie:shell-action:0.1">
            <job-tracker>${nodemanager}</job-tracker>
            <name-node>${nameNode}</name-node>
            <configuration>
                <property>
                  <name>mapred.job.queue.name</name>
                  <value>${queueName}</value>
                </property>
            </configuration>
            <exec>${lineCountShellScript}</exec>
	    <argument>${inputDir}</argument>        
            <file>${FirstShScriptPath}#${lineCountShellScript}</file>
            <capture-output/>
        </shell>
	<ok to="sendEmail"/>
        <error to="killAction"/>
    </action>
    <action name="sendEmail">
        <email xmlns="uri:oozie:email-action:0.1">
              <to>${emailToAddress}</to>
              <subject>Output of workflow ${wf:id()}</subject>
              <body>Creation of Table to Store Datset: ${wf:actionData('create_table_first_time')['NumberOfLines']}</body>
        </email>
        <ok to="end"/>
        <error to="end"/>
    </action>
    <kill name="killAction">
        <message>"Killed job due to error"</message>
    </kill>
    <end name="end"/>
</workflow-app>
